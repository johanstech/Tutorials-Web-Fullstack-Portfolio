import React from 'react';
import ReactDOM from 'react-dom';

import { App } from './App';
import './index.css';

const anchor = document.getElementById('portfolio-root');

ReactDOM.render(<App />, anchor);
